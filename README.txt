This archive contains Cursor Project Rules for the mini ERP MVP project.

Place the `.cursor` folder in the project root, so the final structure becomes:

project-root/
  .cursor/
    rules/
      architecture.mdc
      ui.mdc
      scope.mdc
